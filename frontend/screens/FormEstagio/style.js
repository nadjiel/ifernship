import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
	container: {
		alignItems: 'center',
	},

	main: {
		gap: 25,
		marginTop: 80,
		marginBottom: 10,
		marginHorizontal: 25,
	},
});